# Optional for local dev server proxy. Leave empty in deploy environments.
FRONTEND_PROXY_TARGET=
# OAuth/login direct backend base. Keep as /api for local+deploy parity.
VITE_BACKEND_BASE_URL=/api
# Backward compatibility for old env files (optional)
REACT_APP_BACKEND_BASE_URL=/api
