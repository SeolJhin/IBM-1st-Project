# Local development only
# Proxy target used by src/setupProxy.js
FRONTEND_PROXY_TARGET=http://localhost:8080
# OAuth/login direct backend base. Keep this as /api for local+deploy parity.
REACT_APP_BACKEND_BASE_URL=/api
