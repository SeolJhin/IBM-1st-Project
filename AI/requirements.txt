## Runtime
## Python==3.10.11
## pip==26.0.1


# AI service dependencies (AI/requirements.txt)
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1
PyJWT==2.11.0
openpyxl==3.1.5
python-multipart==0.0.20


# LLM / Orchestration
openai==1.101.0
langchain==0.3.27
langgraph==0.6.6


# IBM watsonx
ibm-watsonx-ai==1.3.34


# Vector DB (Milvus / Zilliz Cloud)
pymilvus==2.5.6

# Voice AI (STT/TTS)
faster-whisper==1.2.0
melotts @ git+https://github.com/myshell-ai/MeloTTS.git@209145371cff8fc3bd60d7be902ea69cbdb7965a
python-mecab-ko==1.3.7
python-mecab-ko-dic==2.1.1.post2
unidic==1.1.0
setuptools<81


# 1. 웹 검색 (Tavily)
tavily-python>=0.3.0      # 또는 requests만으로도 동작 (web_search_tool.py는 requests 사용)
 
# 2. RAG - ChromaDB (Milvus 무료 대체)
chromadb>=0.5.0
pymysql==1.1.2
soxr==1.0.0
 
# 3. RAG - 한국어 임베딩 모델 (무료 로컬, 최초 실행 시 ~500MB 다운로드)
sentence-transformers==2.2.2
 
# ─────────────────────────────────────────────────────────────
# 설치 명령
# pip install tavily-python chromadb sentence-transformers
# ─────────────────────────────────────────────────────────────
 
# .env 파일에 추가할 항목
# TAVILY_API_KEY=tvly-xxxx          ← https://tavily.com 에서 발급 (무료 1000회/월)
# RAG_ENGINE=chroma                  ← chromadb 사용 (기본값)
# CHROMA_PERSIST_DIR=./chroma_db     ← 로컬 저장 경로 (기본값)

# 건물 점검 AI (이미지 비교 + Gemini Vision)
httpx>=0.27.0              # Gemini API HTTP 클라이언트
Pillow>=10.0.0             # 이미지 리사이즈 (damage_analyzer)
opencv-python>=4.9.0       # Before/After 이미지 비교 (image_diff)
numpy>=1.26.0              # OpenCV 이미지 배열 처리