apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: milvus-data-pvc
  labels:
    app: milvus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 20Gi
